---
description: Run the pytest suite with coverage report (optionally scoped to a package)
argument-hint: "[PKG=package_name]"
---

Lance `make test-cov $ARGUMENTS`. Résume le taux de couverture obtenu et signale les fichiers les moins couverts.
