---
description: Run the pytest suite (optionally scoped to a package)
argument-hint: "[PKG=package_name]"
---

Lance `make test $ARGUMENTS`. Si tous les tests passent, dis juste que tout passe avec une coche verte.
