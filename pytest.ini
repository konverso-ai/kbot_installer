[pytest]
# Point pytest-cov at the project coverage config (coverage.py ignores pytest.ini for [run]/[html]).
addopts = --cov-config=.coveragerc

cache_dir = /tmp/.pytest_cache

norecursedirs = 
    core/python/interactivity
