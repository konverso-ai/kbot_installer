# Merge driver for pyproject.toml

Used by git to merge `pyproject.toml` during merge/rebase (see `.gitattributes`).
Register the merge driver in git config (merge.pyproject-merge.driver) to use this directory.
